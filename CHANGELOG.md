# Changelog

## v1.2

本版是在 v1.1 实机验收基础上的小范围安全修订，不改变原有主要菜单结构。

### 修复与改进

- 修复 `[0]` 退出：现在选择 0 后会真正结束脚本，不再重新显示主菜单。
- 修复 `[2]` 迁移菜单：主动识别 Junction / 软链接；已迁移项目显示实际目标路径，并禁止重复迁移。
- “全部迁移”自动跳过已迁移项。
- 扫描结果增加链接提示，避免把 D 盘 Junction 内容误认为仍占用 C 盘。
- `[6]` 收尾 / 回滚在存在 `.old` 备份时优先使用 Junction 的实际目标路径校验，不再只依赖脚本预设路径。

### 延续自 v1.1 的安全修订

- 迁移复制后进行校验；Junction 创建失败时尝试自动回滚原目录。
- 迁移成功后 `.old` 不自动删除；需先验证软件正常，再在 `[6]` 输入 `DELETE` 收尾。
- `.old` 仍存在时可使用 `ROLLBACK` 恢复到 C 盘原目录。
- 默认安全清理不删除 Windows Logs、WER、CrashDumps、`C:\Windows\Minidump`，也不自动删除 Windows Update 下载缓存。
- 报告按“当前剩余空间 - 启动时剩余空间”计算净释放 / 净新增占用。
- 卸载命令尽量保留原始参数；MSI 不默认静默卸载。

### 验收状态

已完成只读 / 非破坏性主流程与安全防护验收，包括扫描、Junction 识别、重复迁移拦截、卸载列表、清理取消、报告、收尾检测与真正退出。

尚未在独立测试环境中完成“全新真实迁移 + DELETE 删除 `.old` + ROLLBACK”等破坏性完整端到端测试。